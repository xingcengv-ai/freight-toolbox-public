# 版本记录

本文件记录每个发布版本新增、调整和修复的功能。

## 未发布

### 新增

- 新建公司首次导入报价表时，自动识别并保存华南或华东 LCL 解析格式。
- 华东报价表复用经过验证的统一解析规则，同时保留华东文件自身的价格数据。
- 支持美东商私卡派表中的沃尔玛等商业仓、邮编和 `WM-` 仓库别名，并按 50KG+、1500KG+、3000KG+ 阶梯查询。
- 补充美东快船海铁纽约卡派的 FBA 包税价格列。

### 修复

- 修复新建公司因解析格式为“尚未配置”而无法导入兼容报价表的问题。

## v0.1.1 - 2026-08-19

### 修复

- 修复公开空白模板使用 XML 命名空间后，批量报价无法生成 Excel 的问题。
- 导出文件继续保持原表头格式、辅助数据列和 100% 工作表缩放比例。

### 新增

- 支持导入英国海运快递派、限时达、卡航的 21-100KG 与 100KG+ 报价。
- 支持英国海卡按 KG 报价及表内仓库代码查询。
- 英国渠道名称支持空格、斜杠和 `&` 的宽松匹配。
- 补充美西普船卡派统配、美东普船限时达纽约卡派和美东普船纽约卡派的按 KG（包税）价格列。
- 上述渠道支持带“（包）”、不带括号和报价表原始空格的名称写法。
- 补充多个美西、美东渠道的按 KG（包税）价格列。
- 支持将“美东”“美中”“美西”作为亚马逊快递派区域简称查询。
- 新增萨凡纳普船卡派专线按 KG 报价，覆盖 `RYY2` 等表内目的仓。
- 对仅存在于目的仓清单、但没有数值报价的仓库给出明确提示，不再误报为仓库不存在。

### 当前限制

- 英国达到 1:250 的重货优惠需要体积信息，当前查询条件不足，暂不自动采用优惠价。
- 英国按方报价和商私卡报价尚未接入当前仅支持 KG 的查询界面。

## v0.1.0 - 2026-08-17

首个可测试版本。

### 报价数据

- 支持从 Excel 导入报价数据，并转换成统一的前端查询结构。
- 支持同一公司保存多个报价版本，可选择当前使用的版本。
- 支持不同公司分别维护报价数据，互不覆盖。
- 导入后的标准化数据保存在当前电脑的本地应用数据中。
- 查询结果保留来源工作表、单元格和价格匹配依据。

### 单票报价

- 支持按渠道名称、目的地和计费重量查询系统单价。
- 支持渠道别名和目的地名称匹配。
- 支持按重量区间选择对应阶梯价格。
- 返回系统单价、计算金额、计费区间及匹配说明。
- 找不到价格时展示明确原因，不静默返回错误结果。

### 批量报价

- 提供独立的批量报价入口。
- 支持从 Excel 分列复制运单号、渠道、目的地、重量和申请单价。
- 按行保持多列数据一一对应，并批量计算系统价格。
- 展示申请单价、系统价格以及两者差额。
- 支持逐行查看未匹配原因。

### Excel 导出

- 支持将批量报价结果生成 Excel 文件。
- 保留业务导入模板要求的固定字段和填写格式。
- 输出渠道、目的地、重量、系统价格及申请单价与系统价格的差额。
- 根据内容调整渠道名称和目的地列宽，完整展示主要数据。
- 导出的工作表默认缩放比例为 100%。
- 导出文件使用公司名称和生成日期命名。
- 公开仓库使用无业务数据的空白格式模板。

### 公司管理

- 支持新增、编辑和管理公司。
- 公司名称可以修改。
- 支持填写非必填的负责人字段。
- 公司选择器支持搜索。
- 每家公司拥有独立的报价表和报价版本。

### Windows 桌面版

- 接入 Tauri 桌面应用框架。
- 提供 Windows 环境准备、桌面预览和安装包生成脚本。
- 支持在 Windows 上生成 NSIS 安装程序。

### 工程与隐私

- 使用 Vue 3、TypeScript 和 Vite 开发。
- 增加项目结构与维护说明。
- 私有报价表和真实测试资料统一存放在 `local-resources/`。
- `local-resources/` 整体被 Git 忽略，不会上传到公开仓库。
- 构建产物、依赖目录和本机临时文件均已加入 Git 忽略规则。

### 当前限制

- 当前仅对已经配置解析器的报价表格式提供自动导入。
- 附加费、超尺寸费、报关费和尾板费等暂不自动计入报价。
- 数据暂存在本机浏览器存储中，尚未接入数据库或云端同步。
